import React from 'react';
import { Container } from '@material-ui/core';

const CryptoCurrencyView: React.FC = () => {
  return (
    <Container style={{ marginLeft: '180px', paddingLeft: '24px' }}>
      {' '}
      CryptoCurrencyView
    </Container>
  );
};

export default CryptoCurrencyView;

